"""Publishing: a YouTube uploader (OAuth + resumable upload + scheduling) and ready-to-upload packages for
Instagram Reels and TikTok, which have no usable self-serve upload API for a local tool (see stubs)."""
